<h1>Search Results for "<%= @query %>"</h1>
<div class="search">
  <% if @projects.empty? %>
    We didn't find any projects tagged with your query! Remember, you can search for multiple tags at once by separating tags by commas. 
  <% else %>
    <%= render "projects/project_list", projects: @projects, scope: "search" %>
  <% end %>
</div>
