<!DOCTYPE html>
<html>
<head>
  <title>Atelier</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <link href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css" rel="stylesheet">
  <link href='http://fonts.googleapis.com/css?family=Snippet' rel='stylesheet' type='text/css'>
  <%= javascript_include_tag "application" %>
  <script src="//netdna.bootstrapcdn.com/bootstrap/3.0.2/js/bootstrap.min.js"></script>
  <%= csrf_meta_tags %>
  <%= analytics_init if Rails.env.production? %>
</head>
<body>
<%= render "nav" %>

<div class="container">
  <% flash.each do |name, msg| %>
    <%= content_tag :div, msg, :class => "alert alert-info", :id => "flash_#{name}" if msg.is_a?(String) %>
  <% end %>  
  <%= yield %>

  <footer>
    <hr />
    <div class="pull-left">
      <%= fa_icon "bug" %>
      <a href="mailto:atelier@cs.stanford.edu?subject=Atelier Bug">Found a bug?</a>
    </div>

    <div class="pull-right">
      <a href="http://hci.stanford.edu">
        <%= image_tag "stanford_hci.png" %>
      </a>
    </div>
  </footer>
</div>

</body>
</html>
